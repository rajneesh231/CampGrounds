<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campgrounds Index</title>
</head>

<body>
    <h1>All Campgrounds</h1>
    <a href="/campgrounds/new">New camp</a>
    <ul>
        <% data.forEach(camp=> { %>
            <li>
                <a href="/campgrounds/<%= camp._id %>"><%= camp.title %></a>
            </li>
            <% }) %>
    </ul>
</body>

</html>